#1 find n n numbers from (1 to n) with n different sequence which are anti fibonacci (pi-1 + pi-2 != pi)
#2 remember when dividing from integer to deal with modular value. 
#3 when comparing two charectar, there ascii value is used